# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# If you need help with YAML syntax, here are some quick references for you:
# https://learn-the-web.algonquindesign.ca/topics/markdown-yaml-cheat-sheet/#yaml
# https://learnxinyminutes.com/docs/yaml/
#
# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

# Site settings
title: Hamzah Jamad
email: hamzahjamad@gmail.com
description: >
  Full-stack developer. I build practical web apps with Laravel, Django, and JavaScript,
  focusing on clear workflows and maintainable code.
baseurl: ""
# Canonical site URL (update if the custom domain is not wired up yet).
url: "https://hamzahjamad.com"
blog_url: "https://blog.hamzahjamad.com"
# Used by the homepage script to show recent posts (REST API first, then RSS via rss2json).
blog_rss_url: "https://blog.hamzahjamad.com/feed/"
blog_feed_limit: 5
blog_excerpt_max_chars: 220
# Optional: https://rss2json.com improves rate limits if the blog REST request is blocked by CORS.
# blog_rss2json_api_key: "your_key_here"

twitter_username: hamzahjamad
github_username: hamzahjamad
facebook_username: hamzahjamad

markdown: kramdown
plugins:
  - jekyll-feed

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
exclude:
  - .sass-cache/
  - .jekyll-cache/
  - gemfiles/
  - Gemfile
  - Gemfile.lock
  - node_modules/
  - vendor/bundle/
  - vendor/cache/
  - vendor/gems/
  - vendor/ruby/
  - README.md
